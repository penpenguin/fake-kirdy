Implemented TDD fixes for result modal timing, Spark texture, stage-cluster terrain visuals, and stage-edge fall recovery. Key files: `godot/scripts/session/GameSession.gd` now sends ResultsScene payload timing (`result_elapsed_ms`, `auto_delay_ms`), applies level visual keys with stage/cluster metadata, and recovers player after falling below camera bounds by respawning at a spawn marker while tracing `player.fall.recovered`. `godot/scenes/ui/ResultsScene.tscn` uses `PopupBackdrop` + `ModalPanel`. Spark now has `godot/resources/assets/images/characters/kirdy/kirdy-spark.webp` and Player.tscn uses it explicitly. Added `flat_room_fall_recovery` replay and `--filter` support in `scripts/run-godot-replay-suite.mjs`. Verification: `npm run test`, focused `results_scene_continue`, focused `flat_room_fall_recovery`, trace summaries for both.